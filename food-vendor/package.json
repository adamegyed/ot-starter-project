{
  "name": "food-vendor",
  "version": "1.0.0",
  "main": "build/index.js",
  "repository": "git@github.com:adamegyed/ot-starter-project.git",
  "author": "Adam Egyed",
  "license": "unlicensed",
  "private": true,
  "engines": {
    "node": ">=8.0.0"
  },
  "dependencies": {
    "@types/restify": "^8.4.2",
    "eslint-plugin-node": "^11.1.0",
    "gts": "^2.0.2",
    "restify": "^8.5.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.6",
    "@types/node": "^14.0.5",
    "@typescript-eslint/eslint-plugin": "^3.1.0",
    "@typescript-eslint/parser": "^3.1.0",
    "eslint": "^7.1.0",
    "eslint-plugin-prettier": "^3.1.3",
    "gts": "^2.0.2",
    "typescript": "^3.9.3"
  },
  "scripts": {
    "build": "npx tsc -p .",
    "start": "node .",
    "dev:start": "yarn run build && yarn run start",
    "gcp-build": "yarn run build",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "yarn run build",
    "fix": "gts fix",
    "pretest": "yarn run compile",
    "posttest": "yarn run check",
    "deploy": "gcloud beta app deploy food-vendor.yaml"
  }
}
